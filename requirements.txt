# Runtime dependencies for the Albertsons store ETL.
# Pinned to stable releases (all published well before the migration).
google-cloud-bigquery==3.25.0
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.20.2
requests==2.32.3
pandas==2.2.2
tenacity==8.5.0

# Testing
pytest==8.2.2
