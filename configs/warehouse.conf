# Data warehouse connection profiles
# Used by: extract_from_snowflake.sh, load_warehouse.sh, data_quality_check.sh
#
# NOTE (migration): the [snowflake_*] profiles all point at the OLD,
# decommissioned Snowflake account. The [bigquery] profile at the bottom is
# the intended target but nothing sources it yet - the scripts still read the
# snowflake profiles. See README "Migration Status / Technical Debt".

# --- LEGACY: primary Snowflake warehouse (DECOMMISSIONED 2026-Q2) ---
[snowflake_prod]
account=alb_prod.us-east-1
host=alb_prod.us-east-1.snowflakecomputing.com
user=ETL_SVC
password=Sn0wf@ll_Etl_2024!
warehouse=ETL_WH
database=RETAIL_PROD
schema=STORE_OPS
role=ETL_ROLE
query_timeout=600

# --- LEGACY: smaller warehouse used for reference/dimension extracts ---
[snowflake_reporting]
account=alb_prod.us-east-1
host=alb_prod.us-east-1.snowflakecomputing.com
user=REPORT_RO
password=R3port_R0_2024!
warehouse=REPORTING_WH
database=RETAIL_PROD
schema=DIM
role=REPORT_ROLE
query_timeout=300

# --- LEGACY: ODBC DSN (used by a couple of ad-hoc reporting scripts) ---
[odbc]
dsn=SF_RETAIL_PROD
driver=SnowflakeDSIIDriver
uid=ETL_SVC
pwd=Sn0wf@ll_Etl_2024!

# --- TARGET: BigQuery (added during migration, NOT yet used by scripts) ---
# TODO(migration): repoint all extract/load/DQ steps here via the
#   google-cloud-bigquery Python client (or `bq` CLI) and delete the
#   snowflake_* profiles above.
[bigquery]
project=albertsons-retail-analytics
dataset=store_ops
location=US
credentials=/opt/albertsons/etl/.gcp/etl-sa.json
